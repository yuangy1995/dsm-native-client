<Page
    x:Class="LanStash.App.Views.NasDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:desktop="using:LanStash.App.Presentation"
    xmlns:nas="using:LanStash.App.Features.NasAdmin"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.KeyboardAccelerators>
        <KeyboardAccelerator Key="F5" Invoked="RefreshAccelerator_Invoked" />
    </Page.KeyboardAccelerators>

    <Grid Padding="24,20,24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid ColumnSpacing="12" RowSpacing="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup>
                    <VisualState x:Name="NarrowHeaderLayout">
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="0" />
                        </VisualState.StateTriggers>
                    </VisualState>
                    <VisualState x:Name="WideHeaderLayout">
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="1060" />
                        </VisualState.StateTriggers>
                        <VisualState.Setters>
                            <Setter Target="HeaderTextPanel.(Grid.ColumnSpan)" Value="1" />
                            <Setter Target="HeaderActions.(Grid.Row)" Value="0" />
                            <Setter Target="HeaderActions.(Grid.Column)" Value="1" />
                            <Setter Target="HeaderActions.(Grid.ColumnSpan)" Value="1" />
                            <Setter Target="HeaderActions.HorizontalAlignment" Value="Right" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            <StackPanel
                x:Name="HeaderTextPanel"
                Grid.ColumnSpan="2"
                Spacing="4">
                <TextBlock
                    x:Uid="NasDetailsTitle" desktop:DesktopLocalization.Bindings="Text=NasDetailsTitle.Text"
                    Style="{StaticResource TitleTextBlockStyle}"
                    AutomationProperties.HeadingLevel="Level1" />
                <TextBlock
                    x:Uid="NasDetailsDescription" desktop:DesktopLocalization.Bindings="Text=NasDetailsDescription.Text"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextWrapping="Wrap" />
            </StackPanel>
            <ItemsControl
                x:Name="HeaderActions"
                Grid.Row="1"
                Grid.ColumnSpan="2"
                HorizontalAlignment="Stretch">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <Button
                    x:Uid="NasDetailsRefresh" desktop:DesktopLocalization.Bindings="AutomationProperties.Name=NasDetailsRefresh.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name"
                    x:Name="RefreshButton"
                    Margin="0,0,8,8"
                    MinHeight="44"
                    Click="Refresh_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE72C;" FontSize="16" />
                        <TextBlock x:Uid="NasDetailsRefreshLabel" desktop:DesktopLocalization.Bindings="Text=NasDetailsRefreshLabel.Text" />
                    </StackPanel>
                </Button>
                <Button
                    x:Uid="NasDetailsRunStorageAnalysis" desktop:DesktopLocalization.Bindings="AutomationProperties.Name=NasDetailsRunStorageAnalysis.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name"
                    x:Name="RunStorageAnalysisButton"
                    Margin="0,0,8,8"
                    MinHeight="44"
                    Click="RunStorageAnalysis_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE9D9;" FontSize="16" />
                        <TextBlock x:Uid="NasDetailsRunStorageAnalysisLabel" desktop:DesktopLocalization.Bindings="Text=NasDetailsRunStorageAnalysisLabel.Text" />
                    </StackPanel>
                </Button>
                <Button
                    x:Uid="NasDetailsRunDeepStorageAnalysis" desktop:DesktopLocalization.Bindings="AutomationProperties.Name=NasDetailsRunDeepStorageAnalysis.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name"
                    x:Name="RunDeepStorageAnalysisButton"
                    Margin="0,0,8,8"
                    MinHeight="44"
                    Click="RunDeepStorageAnalysis_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE721;" FontSize="16" />
                        <TextBlock x:Uid="NasDetailsRunDeepStorageAnalysisLabel" desktop:DesktopLocalization.Bindings="Text=NasDetailsRunDeepStorageAnalysisLabel.Text" />
                    </StackPanel>
                </Button>
                <Button
                    x:Uid="NasDetailsCancelStorageAnalysis" desktop:DesktopLocalization.Bindings="AutomationProperties.Name=NasDetailsCancelStorageAnalysis.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name"
                    x:Name="CancelStorageAnalysisButton"
                    Margin="0,0,0,8"
                    MinHeight="44"
                    Click="CancelStorageAnalysis_Click"
                    Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE711;" FontSize="16" />
                        <TextBlock x:Uid="NasDetailsCancelStorageAnalysisLabel" desktop:DesktopLocalization.Bindings="Text=NasDetailsCancelStorageAnalysisLabel.Text" />
                    </StackPanel>
                </Button>
            </ItemsControl>
        </Grid>

        <InfoBar
            x:Uid="NasDetailsReadOnly" desktop:DesktopLocalization.Bindings="Message=NasDetailsReadOnly.Message;Title=NasDetailsReadOnly.Title"
            x:Name="ReadOnlyInfoBar"
            Grid.Row="1"
            IsClosable="False"
            IsOpen="True"
            Severity="Informational" />

        <InfoBar
            x:Uid="NasDetailsReadWrite" desktop:DesktopLocalization.Bindings="Message=NasDetailsReadWrite.Message;Title=NasDetailsReadWrite.Title"
            x:Name="ReadWriteInfoBar"
            Grid.Row="1"
            IsClosable="False"
            IsOpen="False"
            Severity="Informational"
            Visibility="Collapsed" />

        <InfoBar
            x:Uid="NasDetailsRefreshError" desktop:DesktopLocalization.Bindings="Message=NasDetailsRefreshError.Message;Title=NasDetailsRefreshError.Title"
            x:Name="RefreshErrorNotice"
            Grid.Row="2"
            AutomationProperties.LiveSetting="Polite"
            IsClosable="False"
            IsOpen="False"
            Severity="Warning" />

        <InfoBar
            x:Name="SectionNotice"
            Grid.Row="3"
            AutomationProperties.LiveSetting="Polite"
            IsClosable="False"
            IsOpen="{Binding SectionNoticeIsOpen}"
            Message="{Binding SectionNoticeMessage}"
            Severity="Informational"
            Title="{Binding SectionNoticeTitle}" />

        <Grid Grid.Row="4">
            <Grid
                x:Name="ContentState"
                RowSpacing="12"
                AutomationProperties.LiveSetting="Polite"
                Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <ListView
                    x:Uid="NasDetailsSectionList" desktop:DesktopLocalization.Bindings="AutomationProperties.Name=NasDetailsSectionList.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name"
                    x:Name="SectionList"
                    HorizontalAlignment="Left"
                    ItemsSource="{Binding Sections}"
                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                    ScrollViewer.HorizontalScrollMode="Enabled"
                    ScrollViewer.VerticalScrollMode="Disabled"
                    SelectionChanged="SectionList_SelectionChanged"
                    SelectionMode="Single">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsStackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="MinHeight" Value="56" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="nas:NasDetailsSectionOption">
                            <Grid
                                MinWidth="160"
                                Padding="8,6"
                                AutomationProperties.Name="{x:Bind AutomationName}">
                                <StackPanel Spacing="2">
                                    <TextBlock
                                        FontWeight="SemiBold"
                                        Text="{x:Bind Title}"
                                        TextWrapping="Wrap" />
                                    <TextBlock
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Text="{x:Bind Status}"
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Border
                    Grid.Row="1"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                    <ListView
                        x:Uid="NasDetailsItemList" desktop:DesktopLocalization.Bindings="AutomationProperties.Name=NasDetailsItemList.[using:Microsoft.UI.Xaml.Automation]AutomationProperties.Name"
                        x:Name="DetailsList"
                        ItemsSource="{Binding Rows}"
                        SelectionMode="None">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="MinHeight" Value="72" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="nas:NasDetailsRow">
                                <Grid
                                    Padding="12,8"
                                    ColumnSpacing="12"
                                    AutomationProperties.Name="{x:Bind AutomationName}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="36" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <FontIcon
                                        VerticalAlignment="Center"
                                        FontSize="20"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Glyph="{x:Bind Glyph}" />
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="3">
                                        <TextBlock
                                            FontWeight="SemiBold"
                                            Text="{x:Bind Title}"
                                            TextTrimming="CharacterEllipsis"
                                            TextWrapping="Wrap" />
                                        <TextBlock
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            Text="{x:Bind Detail}"
                                            TextTrimming="CharacterEllipsis"
                                            TextWrapping="Wrap" />
                                        <TextBlock
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            Text="{x:Bind Status}"
                                            TextWrapping="Wrap" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>
            </Grid>

            <StackPanel
                x:Name="LoadingState"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="12">
                <ProgressRing Width="40" Height="40" IsActive="True" />
                <TextBlock x:Uid="NasDetailsLoading" desktop:DesktopLocalization.Bindings="Text=NasDetailsLoading.Text" AutomationProperties.LiveSetting="Polite" />
            </StackPanel>

            <StackPanel
                x:Name="EmptyState"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                MaxWidth="440"
                Spacing="8"
                Visibility="Collapsed">
                <TextBlock
                    FontSize="20"
                    FontWeight="SemiBold"
                    Text="{Binding EmptyTitle}"
                    TextAlignment="Center"
                    AutomationProperties.HeadingLevel="Level2" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="{Binding EmptyMessage}"
                    TextAlignment="Center"
                    TextWrapping="Wrap" />
            </StackPanel>

            <StackPanel
                x:Name="ErrorState"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                MaxWidth="440"
                Spacing="12"
                Visibility="Collapsed">
                <TextBlock
                    x:Uid="NasDetailsErrorTitle" desktop:DesktopLocalization.Bindings="Text=NasDetailsErrorTitle.Text"
                    FontSize="20"
                    FontWeight="SemiBold"
                    TextAlignment="Center"
                    AutomationProperties.HeadingLevel="Level2" />
                <TextBlock
                    x:Uid="NasDetailsErrorMessage" desktop:DesktopLocalization.Bindings="Text=NasDetailsErrorMessage.Text"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextAlignment="Center"
                    TextWrapping="Wrap" />
                <Button
                    x:Uid="NasDetailsTryAgain" desktop:DesktopLocalization.Bindings="Content=NasDetailsTryAgain.Content"
                    MinHeight="44"
                    HorizontalAlignment="Center"
                    Click="Refresh_Click" />
            </StackPanel>

            <StackPanel
                x:Name="UnavailableState"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                MaxWidth="440"
                Spacing="8"
                Visibility="Collapsed">
                <TextBlock
                    x:Uid="NasDetailsUnavailableTitle" desktop:DesktopLocalization.Bindings="Text=NasDetailsUnavailableTitle.Text"
                    FontSize="20"
                    FontWeight="SemiBold"
                    TextAlignment="Center"
                    AutomationProperties.HeadingLevel="Level2" />
                <TextBlock
                    x:Uid="NasDetailsUnavailableMessage" desktop:DesktopLocalization.Bindings="Text=NasDetailsUnavailableMessage.Text"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    TextAlignment="Center"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
