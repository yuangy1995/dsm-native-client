<!-- doc-role: status -->
<!-- last-reviewed: 2026-09-21 -->

# 当前开发进度

本页只维护当前实现、验证缺口与下一步。2026-09-21 本次 Windows 对齐及 Mac 修复的开发/自动化范围已完成，见[最终范围核对](../development/WINDOWS_PARITY_FINAL_REVIEW_ZH.md)：Windows Build 的 3929 项测试、x64/ARM64 构建均通过，仓库/文档门禁通过；本地隔离 Shell 检查和中英各 47 个云盘场景通过。最新包 windows/dist/20260921-190403 已核查，未安装/启动。Mac Apple Build 成功，52 个 Apple 文件与验证提交一致。本次 Windows/Mac 测试分支和工作树已清理，源码保留本地 main，未提交/推送 main。真实 NAS/设备验证后置；Android 已知目标目录创建版本不一致属范围外问题，不宣称五端全部通过。
本轮将 Android、iPhone、iPad、Windows 正式 Photos 入口对齐 macOS，并整理照片文档；目标平台自动化与真实设备验收分别记录。
详细范围见[平台矩阵](PLATFORM_MATRIX.md)，优先级见[路线图](ROADMAP.md)，证据等级见[验证规则](../quality/VERIFICATION_LEVELS_ZH.md)。

## 五端状态

2026-09-16 用户授权的 macOS 下载入口纠正：`force_complete` 已按“结束下载并保留未完成
文件”说明，不再冒充删除数据；源码/双语检查完成，Swift/macOS 构建和实机仍待验证。

| 平台 | 源码 | 自动化 | 真机 / 真实 NAS | 发布 |
| --- | --- | --- | --- | --- |
| macOS | 已接入 Synology Photos 主流程、按能力开放个人空间单项删除；其余模块保持各自门禁。 | 共享 Swift 回归和本机 arm64 Release 测试包已有执行记录；提交级 CI 仍以 Checks 为准。 | 已有 Photos 读取及单张合成图删除证据；App 异常路径、正式签名、Finder/File Provider 仍待验证。 | 提供独立临时签名测试包；不等于正式发布批准。 |
| iPhone | 移动范围内的登录、Files、Photos、受限 Chat、Download Station 与只读 NAS 摘要已在通用工程中。 | Apple Build 覆盖共享包、通用工程编译及独立 iPhone 模拟器合成回归；精确结果以 GitHub Checks 为准，不替代系统选择器与真机验收。 | `PENDING_USER_VALIDATION`：设备登录、选择器、网络切换、VoiceOver 与真实套件行为。 | 未发布；随 Apple Beta 验收入口统一判断。 |
| iPad | 与 iPhone 共用领域与网络层，保留双栏、键盘与宽屏适配路径。 | Apple Build 分别执行 iPhone、iPad 模拟器合成回归并保留结果；精确结果以 GitHub Checks 为准，不以 iPhone 通过代替 iPad。 | `PENDING_USER_VALIDATION`：iPad 启动、分栏/宽屏、键盘、动态文字、VoiceOver 与真实 NAS。 | 未发布；不以通用编译替代 iPad 验收。 |
| Android | Compose 正式照片入口已使用 Synology Photos；原有备份独立保留，质量基线和后台语义不变；Container 未验证写操作继续关闭。 | Android Build 覆盖 JVM 单测、Debug APK、Release/R8、androidTest APK 构建与 lintDebug；写操作、页面五态、点击目标、动效、结构债务、本地化、fixture 与契约门禁可在仓库运行。精确提交的结果以 GitHub Checks 为准。 | `PENDING_USER_VALIDATION`：真实登录、证书、后台、真实 NAS、危险写和多设备辅助功能。 | 未发布；自动化构建覆盖不替代仪器执行、签名、安装或升级回滚验收。 |
| Windows | WinUI 正式照片入口已使用 Synology Photos；保留领域、基础设施与 Cloud Files 路径；未授权写和未验证系统集成继续关闭或只读。 | Windows Build 在托管 Runner 覆盖 xUnit、WinUI x64 与 ARM64 构建；精确提交的结果以 GitHub Checks 为准。 | `PENDING_USER_VALIDATION`：Windows 设备、Explorer/Cloud Files、通知、安装生命周期和真实 NAS。 | 未发布；不改变当前 unpackaged 形态、签名或程序集引用。 |

## 当前工作与边界

2026-09-20 容器生命周期源码推进：Mac 参数从单 id 改为新鲜清单绑定出的 name，
删除固定 force/preserve_profile=false；启停/重启增加确认与目标快照，状态回查和
启动时间变化用于重启判断。两端均排除套件及托管项目实例，未知结果只核查不重发。
Windows 四操作核心和原生管理窗口已接通，支持筛选、多选确认、逐项结果与恢复核查；
明确结果后自动刷新，未确认或认证问题中止后续批次，不重发。Mac/共享 Swift 新增测试
未运行（本机无 Swift），未生成 Mac 修复包。镜像管理仍是独立未完成项。
重启过渡态限制已修正：两端允许停止/重启，Windows 显示“正在重启”并纳入运行筛选；
启动/删除仍要求稳定停止，完成核查仍要求退出过渡态，重启还需启动时间变大。
Windows 重解析点测试改用真实目录联接，不再要求本机符号链接特权；根目录/子目录
拒绝断言保留并检查目标完整性。
只修改测试夹具，不修改生产权限或上传保护。Mac 新增回归未执行。
Windows 镜像删除已接标签展开、多选确认、完整清单/容器占用预检与只读恢复；
请求按仓库/标签合组，裸镜像按身份删除，标签删除不要求整个镜像 ID 消失。
Windows 已接文件批次/核对、VMM 多台电源、VM/映像删除、任务清理、创建后开机、五档优先级及系统信任连接的独立控制台；目录上传已移除 20/20/8 限制，支持后台可取消准备及完整确认，最新全量 3374 通过。固定证书控制台仍有限制；Swift 回归未运行、未生成 Mac 包。
Mac 镜像删除及拉取任务追踪已补源码，Swift/App 测试未运行、未打包；恢复限本次连接。

Windows 解压最新已接完整归档分页/子目录、目录结构/展开、同名子文件夹、独立
覆盖确认和现有目标权限检查；结果核查深入子目录并核对已知大小。扩展名入口对齐
macOS，NAS 预检失败不提交。已按源码纠正“另选目标目录”的假定缺口：macOS
prepareExtraction 固定 currentPath，Windows 保持相同语义；不因旧清单另造功能。

Windows 解压密码、自动/手选编码和密码错误原地重试保留；任务未完成/失败不凭
输出出现报成功。本波双语各 13 场景通过，全量 2845 通过/1 项符号链接权限失败/
0 跳过；真实归档内容、密码及复合扩展名目录命名需用户验证，不等同已经实测。

2026-09-19 Windows 高级压缩已接 ZIP/7z、四档级别、可选密码与正确扩展名，沿用
现有权限与冲突检查，密码不保存在恢复状态；33 项聚焦回归通过，任务未完成/失败
不再因目标文件出现就报成功，必须有任务完成和文件回读两项证据。2026-09-20 已
移除压缩 20 项客户端限制；Mac 压缩/解压固定当前目录，不另造跨目录缺口。
用户已重新登录，容器生命周期只读核查已恢复：官方静态 start/stop/restart 使用
name，delete 还带 force/preserve_profile；与 Mac 单 id 不符。下一切片按记录修复
Mac 并接 Windows，未执行任何真实写操作，不把静态参数当作写行为已验证。

2026-09-19 用户要求：真实验证不计入剩余开发，交付的验证版本不能靠硬编码关闭已
实现功能；接口/权限/确认/防重复仍保留。VMM 电源/设置/创建三项固定关闭门已移除，
正式入口合成回归通过；NAS 专用 21 项及容器网络 2 项固定关闭条件也已移除，系统
管理保留明确管理员与接口/确认/回读，容器保留套件权限边界。Chat 高级环境白名单
和系统套件管理读取依赖已移除，改按绑定会话及对应接口能力开放，290 项 Chat 聚焦
回归通过。操作授权与验证证据分开，Agent 不执行真实高危写，未实现能力仍不开放。

最新 Windows 独立包为 `windows/dist/20260921-112345` 的 x64/ARM64 自包含 Release，
新增有远端缺失证据的本地投影清理及保留/移除反馈，保留目录新增和稳定名称等此前功能。双架构 publish、归档标记及 SHA256
核对通过，未安装/启动；全量 3745 通过/0 失败/0 跳过，testsRun=true；双语原生与真实系统隔离删除回调检查通过，真实 NAS 端到端仍待验。
不是正式发行或 Mac 修复包；Mac 挂载已补本次连接内未知恢复/明确继续及窗口源码未编译，目标构建和修复包仍待完成。

Windows 网络改名/删除、映像导入、空白盘高级创建及公开克隆创建已接源码与合成闭环；云盘已接跨回调版本/大文件读取及已浏览文件刷新，仍剩目录增删同步、修改写回恢复及完整 Mac 修复包。
控制台根路径/单段别名、云盘管理入口及通知生命周期已接通；中文最新原生场景完整通过，但资源管理器读写不在管理页场景覆盖内。云盘存储扩展已获批准并开始实施；Mac 仍缺
修复目标构建和测试包。容器主要操作、VMM 基础创建/创建后开机/优先级不再计为
未实现。该清单不是精确功能个数或完成百分比；真实 NAS/设备实测单列，不并入剩余
开发。旧“4 类开发/2 类交付”和“向导未接入”描述已由源码进度取代。

优先处理 macOS 1.0.9 用户反馈：文件下载/照片保存中间文件移入应用临时区并采用
系统协调保存，Mac 照片支持面板已确认的替换；删除预检不再依赖无关媒体详情。
导出复制不再占用界面线程，取消前核查；删除明确权限/会话拒绝不再伪装为永久待核查。
上述下载/照片切片已通过 GitHub Swift 回归及临时签名打包，包在 apple/Apps/DsmMac/dist/github-download-photo-20260921；没有本地挂载扩展，真实 NAS 仍待用户测试。
其他 Mac 同类 API 修复尚未纳入该包；继续云盘与后续 Mac 集成，不把本切片当整体完成。

Windows VMM 已接官方任务列表/进度与独立原生任务页；可见时刷新未结束/未读到状态
的任务，离页取消，读取失败保留旧结果。“已结束”不等于创建成功。源码及合成回归
已补已结束任务清理及证据保护、冻结范围和只读恢复，真实 NAS 待验；映像导入已接源码和原生流程，真实结果另验。
包含最终任务页的 Windows x64/ARM64 独立包已生成，未安装或启动；Mac 修复验证
等待期间推进独立 Windows 工作，不将其构建结果当作 Mac 验证。

Windows VMM 已接公开 create→任务绑定→资源核查→基础设置与最终回读及原生向导，
未知不重发，核查与继续写入分开，按会话/接口能力开放用户测试；映像克隆来源、
ISO/固件/系统预设已在空白盘高级模式接通，创建后开机与任务清理已接。公开存储容量单位及已用字段已纠正。

用户已允许兼容接口扩展及后续非高危操作，代码实施不再等待该项审批；真实 NAS 高危
写仍逐项确认。Windows VMM 单机及多台开机/正常关机/强制关机已有公共 API 核心、原生确认
与状态核查，接受后正常转态持续等待，未知停止剩余且不重发；只有合成证据，已按授权开放用户确认操作。

Windows VMM 基础编辑已接公开 Guest.get/set v1：名称、说明、CPU、内存和三态自动启动，
包含原值确认、停机硬件限制、部分保存核查及电源互锁；原生双语合成流程已覆盖。
基础保存已按会话/接口开放，真实 VM 配置变更未验证；高级编辑和控制台仍待做。

macOS 共享 VMM 修改已补齐全部发送字段的精确回读核查，未知提示不再要求重复保存；
新增 Swift 回归未运行（本机无工具链）。内部自动启动语义仍待官方页面核查，不把
字段回显等于实际启动行为；此波没有新真实 NAS 证据或开放写门。

Windows 文件拖放移动已改用页内一次性票据和现有批量确认，拒绝普通文本/伪造/过期
来源，目标权限不再固定为可写；全部回读确认后才提供基于移动后条目的撤销。原生
合成回归已覆盖确认与撤销，真实 NAS 待验。共同的命名空间资源键转换错误同步修正。

Windows 传输中心生命周期已有合成回归：隐藏/关闭不再持锁等待首次 NAS 读取，两个活动
刷新器销毁前即拒绝重启，重复销毁等待在途读取；不停止真实 NAS 任务或改变传输协议。
传输中心已接两个来源的“加载更多”，失败保留已读内容并按同一游标重试；自动刷新
保留展开范围，列表按稳定标识更新，不再每半秒整体重建。真实 NAS 长列表仍待验收。

Windows VMM 已移除资源/保护/事件响应的客户端 200 项截断，并修正内部日志 JSON
编码和能力元数据校验；仅为源码/合成证据，日志仍取已记录首批 1000 项，不代表全部
历史或生命周期/控制台功能完成。

Windows 容器映像仓库已接首批搜索、标签读取/筛选/选择的只读流程及合成回归；
错误不作空结果，切换目标取消旧请求，拉取仍未开放。Apple 共享标签字符串数组
被静默丢弃的同类问题已修源码并补测试，Swift/macOS 回归待执行。
本轮官方网页又确认 Image.list 的完整分页和布尔参数，Windows/macOS 已有请求已修；
下载任务状态只有静态线索，公开下载模型尚未新增，不将启动回执当作下载完成。

Windows 容器网络已有驱动、关联数量/名称、子网/网关/IP 范围与 IPv6 展开详情；
只使用既有列表请求，缺失关联不补零。创建核心已有配置校验、重名防护与不重放
回读及原生完整表单/确认/核查已有合成证据；删除已接对象数组、占用保护、多选确认和
不重放核查，仍仅为合成证据，写门关闭，Apple 计数修正待 Mac 回归。

Windows 电源计划已有固定 v1 只读列表、筛选、未知时区/状态和不完整提示的合成证据；
API 仍为 static 候选，真实读取待验收，没有保存入口。Apple 解析修正待 Mac 回归。

Windows 外接存储已有 USB/eSATA 独立读取、容量/状态/筛选及局部失败原生展示，
仅有 static/合成证据，不开放弹出；Apple 同类严格读取修正待 Mac 回归。

Windows 内存压缩已接 v1 只读摘要，未知不补关闭或零容量，无设置开关；接口仍为
static 候选，真实读取与 Apple 共享解析回归待验收。

Windows Chat 刷新时序回归已改用可控时间和请求屏障，保留连接前轮询后的必要补读；
连接校准/断开回退不再依赖短墙钟窗口断言。生产刷新间隔与协议不变，真实网络仍待验收。

Windows NAS 终端/代理已有原生查看/编辑表单、输入校验和风险确认；读取已修正字段、
版本及失败被当成关闭的问题。基线绑定的保存/逐字段回读核心已补合成测试，尚未完成
真实行为验收，生产写入口保持关闭；未知保存可在同客户端重建页面后只读核对。
通用编辑器未知结果先重新读取、不能显示成功；PRI 点分属性键的跨模块取词故障也已修复。
当前命令与验收边界见
[Windows 实施账本](../development/WINDOWS_MACOS_PARITY_DEVELOPMENT_PLAN_ZH.md)。

Windows 文件服务已改为六组固定契约，字段可用/失败标记区分未知与关闭，FTPS 独立显示；
六组基线保存、依赖校验、确认及整体回读已有源码/合成闭环，生产门仍待真实行为验收。

区域/时间已补 Windows 固定读取、基线保存/条件校时核心和原生编辑，生产行为门仍关闭；
Mac 共享读取同步修正未知模式误作手动、
缺失时分秒补零和数值/日期边界。Windows 合成回归可运行；新增 Swift 回归因缺工具链
未运行，不能外推为 Mac 通过；改时和校时仍未在真实 NAS 验收。

Windows 物理网卡已有列表/详情、单目标 configs 保存及重连核对核心；不猜测缺失值，
新地址拒绝旧会话并要求同 NAS 确认。源码/合成闭环不等于真实网络变更验收，生产门关闭。

Windows 安全设置已有四分区读取、基线保存和防火墙任务核心及原生编辑；旧保存不再
忽略内部结果而误报成功。未知任务不盲目清理或重发，生产行为门仍关闭，真实验收待补。

Windows 硬件/UPS 已有六组 v1 基线写、LED 设置/更新、可信字段校验和整体回读核心，
原生编辑按确认快照保护输入；生产行为门仍关闭，不进行真实物理副作用测试。

Windows DDNS 固定读取、四操作核心与原生编辑/确认/反馈/恢复调用链已有合成证据，
确认绑定当前输入、密码用后清除、未知结果不重放；生产门关闭。Apple 适配器与 Mac
界面层均纠正拒绝/未知被旧记录覆盖为成功的问题，Mac 回归与真实 NAS 写验收待完成。

Windows 套件固定读取、三操作核心及原生确认/反馈/恢复已有合成证据；恢复包含已从
目录消失的目标，启停/卸载能力分离，生产门关闭。Mac 解析失败关闭修正仍待回归。

Windows 账号/群组读取、新建/编辑/删除核心及原生管理已有合成证据；组关系仅明确
修改时提交，确认随输入失效，密码清除和未知恢复已接。写门关闭，Mac 修正仍待回归。

Windows 电源 v3 预检、原生确认和接受/未知反馈已有合成证据，不推断最终设备状态；
再次操作需新登录及用户设备核对，生产门关闭。
Windows 连接读取/断开核心及原生确认/恢复已有合成证据，当前或未知当前状态要求
额外确认，共享原始目标不能绕过阻止；写门关闭，Mac 修正待回归，未执行真实断开。

Windows 任务计划读取、命令和 v4 创建/编辑核心已有合成证据；双基线及配置回读不
以名称存在代替保存成功，运行只确认接受。任务管理状态层已有确认失效、按需详情/
输出、迟到响应隔离及只核查恢复测试；原生列表/编辑/四命令确认/记录输出已接线并
通过合成场景，生产门关闭、真实任务未验收，Mac 修正待回归。

Windows 硬盘检测 v1 目标/状态/最近历史读取已有合成证据，旧猜测写入口已关闭；
启停核心及原生选择/确认/历史/恢复已有合成证据，运行中只读刷新、写门关闭。
Apple 身份/许可/状态及设备/代次缓存绑定、旧状态误报成功修正待 Mac 回归，真实检测未验收。

Windows 远程访问固定 v3/v1 读取、基线保存/中继保护/不重放恢复核心已有合成证据，
原生表单已有独立字段/确认/部分与未知恢复合成证据，写门关闭；Apple 修正待 Mac 回归。

### macOS

- 本轮四端照片对齐以 macOS 为只读参考，没有修改 macOS App 或 Apple 共享 Package 生产源码；macOS 自身后续开发仍按用户授权执行。
- Photos 当前能力、五端迁移和待完成写功能统一见[照片计划](../development/NATIVE_DSM_PHOTOS_DEVELOPMENT_PLAN_ZH.md)。五端正式入口不回退 File Station；旧文件导入／备份通用组件和回归保留，不再作为 Photos 浏览的降级。
- 个人空间删除按用户授权跨 DSM／Photos 版本依接口与实际权限开放；确认、防重复和结果回读不变。其他版本仍未验证，开放不等于通过验收，也不解锁其他写操作。
- 已有真实 Photos 读取及单张新增合成 PNG 删除／刷新回读证据，见[环境索引](../api/discovery/environments/INDEX.md)；不能再笼统描述为“没有 NAS 验证环境”或“所有危险写都没有证据”。
- 本机测试包沿用独立身份及临时权限，不包含本地磁盘挂载、不自动安装或启动。正式签名、公证、升级和 Finder 验收仍须独立执行，见[Beta 就绪报告](../quality/MACOS_BETA_READINESS_ZH.md)。

### Windows 原生重建

- 普通链接创建已复用 NAS 目录选择，Windows/Apple 按 destination 使用 v2；公共 fixture
  已纠正，Android 对应 v1 测试配置存在待同步风险，未宣称跨端门禁全部通过。
- 任务文件创建已新增保存目录和遮蔽解压密码，并与 Apple 同步纠正 destination 的 v2 要求。
  同期修复图片预览晚到进度覆盖就绪快照的竞态；Windows 自动化与 Mac 待验收分开记录。
- Download Station 已新增默认文件夹/限速/eMule/自动解压/计划设置编辑，Info v2 位置字段、两组件确认与未知结果核对已补回归；真实保存仍待验收，下载其余差距继续推进。
  后续已接通批量暂停/继续/移除，以及按官方语义结束下载并保留未完成文件；逐项核对与继续确认已接线，不代表实际文件副作用已验证。

- 用户已明确同意兼容接口扩展；Chat 提醒、定时、无附件投票、群公告、批量关闭/转发/本人消息删除及新联系人转发已进入实现和本地验证。
  历史合并/本地已读保留；已新增实时事件回读与 5 秒轮询降级，真实 NAS 未验证。最新进度见[持续实施账本](../development/WINDOWS_MACOS_PARITY_DEVELOPMENT_PLAN_ZH.md)，整项目标仍未完成。

- 用户已要求完成剩余业务差距：第一波补齐 Chat JSON 创建/成员与结果不明保护、Container
  完整清单/日志分页、Files 搜索完整生命周期及离页取消；当前 NAS 只读能力元数据已核实。
  实际测试/构建与仍未完成的全范围见[持续实施账本](../development/WINDOWS_MACOS_PARITY_DEVELOPMENT_PLAN_ZH.md)。

- 照片加载与跨模块会话/参数偏差已补回归修复；完整功能对齐仍有明确缺口。实际检查范围、
  回归结果、本机权限限制及 NAS 待验收见[请求一致性复核](../development/WINDOWS_API_PARITY_AUDIT_ZH.md)。

- 按用户提供的 macOS 浅深色截图重建 Shell、设备侧栏、文件工具栏／网格／详情、登录和 NAS 存储概览；其余模块复用现有业务页并统一主题。
- 修复 unpackaged 启动和资源发布、容量格式、分页集合重建及 NAS 分类重复刷新；已有本地 x64 原生合成回归和 x64／ARM64 自包含构建记录。
- 完整 macOS 功能对齐仍未完成；目标平台回归、权限受限测试、系统／NAS 缺口及本地包说明集中在[重建账本](../development/WINDOWS_NATIVE_REBUILD_ZH.md)。不以截图或可启动的包代替业务验收。

### 其他平台

- Apple 移动复用 Photos 网络与业务模型，使用触控筛选、原生预览和系统保存／分享；iPhone、iPad 模拟器测试均进入 Apple Build，但系统选择器、宽屏和辅助功能仍须设备验收。
- Android 保留 `AppViewModel`／`DsmRepository` 兼容门面，传输、照片备份、Chat 读取和 NAS 摘要已由现有协调器／特性模型管理。写流程保持原有安全边界，后台唯一名称和存储格式不因拆分改变。
- Android 质量基线由机器数据生成；结构债务 ID 和行数 ratchet 不得借移动文件或新增例外放宽。完整 JVM、Release/R8、androidTest APK 与 lint 交托管 Runner；APK 构建不等于仪器执行。
- Windows 新照片页使用既有 API 客户端及连接／证书上下文；时间线、12 类筛选、共享读取、媒体预览、原件保存和个人单项删除有专用回归；WinUI x64／ARM64 由 Windows Runner 验证。
- 其他端的详细范围与迭代顺序分别由[Apple 移动计划](../development/APPLE_MOBILE_MACOS_PARITY_DEVELOPMENT_PLAN_ZH.md)、[Android 计划](../development/ANDROID_CLIENT_COMPLETION_PLAN_ZH.md)、[Windows 计划](../development/WINDOWS_MACOS_PARITY_DEVELOPMENT_PLAN_ZH.md)维护，不在本页重复展开。

## 待验证条件

以下使用 `PENDING_USER_VALIDATION`，只约束依赖该条件的能力，不阻塞独立源码和自动化切片。

| 条件缺口 | 影响范围与当前边界 | 验证出口 |
| --- | --- | --- |
| 正式 Apple 签名、公证与系统注册 | macOS 正式分发、Finder/File Provider；临时包不替代 | 按[桌面云盘验收矩阵](../development/MACOS_DESKTOP_CLOUD_DRIVE_SIGNED_ACCEPTANCE_MATRIX_ZH.md)执行 |
| 尚无对应目标的套件／写场景证据 | 未覆盖的认证、Chat、管理写、权限及恢复；不能用 Photos 结果类推 | 按[私有 API 发现规则](../api/discovery/README.md)逐切片记录授权、版本和脱敏结果 |
| Photos App 异常路径与其他版本 | 已开放个人空间删除；断网、重启、权限变化与恢复未验证 | 可丢弃照片取消／删除／刷新／重启；未知结果先查官方页面，反馈脱敏错误 |
| 移动端与 Windows 系统行为 | 选择器、后台、Explorer、通知、辅助功能等 | 按目标平台专项计划在真实设备或适用模拟器验证 |
| Android 仪器与发布生命周期 | 系统版本矩阵、Doze、进程恢复、签名、安装、升级与回滚 | 受控设备执行；不把构建结果当作仪器或发布验收 |

发布、隐私、权限变更审批和证据规则统一见[验证等级](../quality/VERIFICATION_LEVELS_ZH.md)、[安全基线](../security/SECURITY_BASELINE.md)及各平台验收文档。`Documentation & Quality Preflight` 只验证文档和质量门禁，不是完整发布验收。没有新公开发布批准；本机测试包与正式发布必须区分。

## 下一步

1. 按实际反馈继续修复 macOS Photos 主流程；新增上传、整理、分享写入单独拆分契约和授权，不借反馈扩大写入范围。
2. 继续各平台已有计划，Photos 后续以各端真实使用反馈为依据；新增写切片与 Android 结构收敛保持独立范围，避免共享热点交叉修改。
3. 在具备条件时补正式签名、系统集成、目标设备与真实 NAS 验证；按功能记录缺口和脱敏结果，不设置全项目等待。

历史决策和执行记录见[跨端历史](../archive/2026-h2/CROSS_PLATFORM_PARITY_HISTORY.md)、[Android 历史](../archive/2026-h2/ANDROID_ALIGNMENT_HISTORY_82_89.md)、[发布验证历史](../archive/2026-h2/RELEASE_VALIDATION_HISTORY.md)。
