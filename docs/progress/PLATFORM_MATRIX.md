<!-- doc-role: platform-matrix -->
<!-- last-reviewed: 2026-09-21 -->

# 平台功能矩阵

2026-09-21 最新复核：Windows 指定文件夹及全部共享内部读写/改名移动/删除确认和缓存
回收已接，挂载根和共享根保护不变；3929 项测试、Shell 隔离检查、中英各 47 个云盘
场景和 190403 双架构包验证通过。[最终范围核对](../development/WINDOWS_PARITY_FINAL_REVIEW_ZH.md)
已汇总；Windows Build 已通过：3929 项测试、x64/ARM64 均成功，测试分支已清理。下方较早
“写回未接通/外部删除一律拒绝”段落为历史波次，当前状态以本段与 Windows 账本为准。
macOS 修复包已完成 Apple Build，临时签名包不含挂载扩展；真实 NAS/系统验收独立。

用户已授权正式提交前补齐 Android 下载创建兼容：指定目录使用 v2，无目录使用 v1，
版本不支持时零提交拒绝；1424 项 JVM 测试及完整构建/lint 通过。后续正式提交证据见
[提交验收记录](../development/PARITY_SUBMISSION_VALIDATION_ZH.md)，不改变五端既定范围。

2026-09-21 Windows 已接远端缺失确认后的本地投影清理：目录差异加精确存在性读取，
无未决写且原生身份/同步状态符合时才清理；离线保留和本地修改不删除，普通外部删除
仍拒绝。双语界面与隔离系统回调验证通过，真实 NAS 联调另验；修改写回仍未接通。

2026-09-21 Windows 云盘目录新增已接到显式刷新，名称映射持久化为独立同步记录版本 2，
后加大小写冲突不改旧名，重启后保持定位；版本 1 可读，旧包拒绝改写版本 2。原生隔离
检查通过，其他四端无数据迁移；远端移除/重命名旧条目清理与写回仍未完成。

2026-09-21 Windows 文件接口依赖复核修复固定 v2/getinfo 在传输层被误拦截的问题，
并统一浏览/变更/回收/归档/共享的 ACL 与 adv_right 权限解码。明确拒绝优先，未知不
授权；仅采用已有公开契约，其他四端接口与源码不变。云盘存在性读取已补，目录增删
动作仍在实施，不把列表缺项或读取失败当删除依据。

2026-09-21 Windows 控制台已按官方脚本补 alias/app_id，并修复托管语言加载缺口。
域名、单段别名、IPv4/IPv6 均经过原生合成验证；网页保持 connect-src none、无
NAS Cookie，语言只读通过受限桥接。实际门户部署/VM 画面另验，其他四端实现
未改；全量 3633 通过，控制台原生中英文各 22 场景通过，双架构独立包为
20260921-020146。随后中文全量原生通过，精确报告见持续账本；整合审计仍发现云盘源码差距，
不能沿用“只剩整合/Mac 包”的结论：Windows 后续已补持久化版本、分段/大文件读取，
后续又接入已浏览文件内容刷新、固定偏好恢复与取消，隔离原生接口检查及双语界面
回归通过；仍缺远端目录增删/重命名同步及修改写回/恢复 UI。待同步日志核心已实现；
Mac 下载/照片修复局部包已通过 GitHub 构建并取回，其他 Mac 修复尚待整合；
局部测试分支已删除，代码保留本地 main。真实设备验收另列，不冒充源码缺口。

2026-09-21 Windows 公开磁盘映像克隆创建已补闭环：冻结源 ID，绑定同次任务的
严格完成结果与新 guest_id，设置前后校验磁盘/网卡/MAC 快照；不再无条件停在
VerifyImageSource。未知、未完成任务和硬件漂移仍阻止写入，确认后开机流程与
任务过期后的只读恢复已回归。该检查依赖公开克隆契约，不宣称内容哈希比对；
真实 Guest OS 启动另验，其他四端源码未改，Android 既有处理未标为已修。
全量 3609 通过，创建原生中英文各 40 场景通过，双架构独立包为 20260921-005335。

2026-09-21 Windows 空白盘高级创建已接系统预设、固件、安装 ISO 与完整任务/硬件
核查，复用原窗口及创建/电源协调逻辑，按实际能力提供用户测试入口。全量 3593
测试通过，创建原生中文/英文各 36 场景通过，双架构独立包 20260921-000522。
默认磁盘映像创建保留公开路径，其后续闭环见上段；本波不是整个对齐目标完成。
实际 App 创建/启动及 Mac 构建仍分列，不将官方页面隔离样本外推为 App 实机通过。

2026-09-20 专用隔离 VM 的内部创建已取得有限行为证据：task_id 与聚合任务的
guest_id/请求身份关联，1 CPU、512 MiB、10 GiB、UEFI、断网、无 ISO、关机已
回读；不是 Windows 高级向导完成或历史设备兼容升级。发现内部 get/list v2、
get_setting v1 读内存 KiB、create/set 写 MiB。Windows 内部优先级/高级读取和
Mac 显示/保存核查已修源码；公开 API 的 MiB 处理不变。Mac 创建 allocated_size
改为原生整数，Apple 测试已补但无 Swift/Mac 构建；移动端公开读取不变，Android
只记录影响不改代码。最新实测与自动化范围见高级创建观察及 Windows 持续账本。

2026-09-20 Windows 已接公开 VMM 映像导入：NAS 来源路径、disk/iso/vdsm、多目标
存储、确认和进度、任务/新映像核对、同会话只读恢复与重复提交保护。沿用公开
create-image fixture，不新增私有契约；中文和英文原生各 13 场景通过，真实 NAS
导入待用户验证。Mac 当前无对应导入入口，未把本波称为复制已存在的 Mac 功能，
其他四端不扩范围；原承诺的高级 VM 创建与发布/构建缺口仍保留。

2026-09-20 Windows 控制台已补托管 WSS 握手基础，复用 Chat 的原 HttpClient/证书
上下文；本机 TLS 测试证明固定证书通过、未批准/变更证书拒绝且未发送凭据。
后续已接资源读取与二进制消息桥，固定证书根路径连接按实际能力提供用户验证
入口，网页无 NAS Cookie，CSP 禁止直接连接。全量 3485 通过，托管 WebView2
合成往返/关闭通过；最终双语回归与分发包见持续实施账本。实际 NAS noVNC
及键鼠未验证；当时未核实的别名路径已由 2026-09-21 官方资源证据与合成回归补齐，
真实门户部署仍待验。其他四端实现与兼容证据不变。

2026-09-20 Windows 虚拟网络已接单项改名、多选删除和只读核查；完整网络/关联 VM
快照、明确确认、固定 v1、写后同源核查、未知不重放及 VMM 模块互锁已补合成回归。
对应 Mac 的现有 name-only 更新，不额外改变 VLAN/接口拓扑。全量 3439 通过，
双语原生各 19 场景通过，独立 x64/ARM64 包为 20260920-195437。真实网络写及 VM
连接影响仍待用户验证，不提升兼容等级。

2026-09-20 VMM 网络官方只读核查已恢复：当前版本 Network.list/get v2 与
list_avail_interface v1 成功，set/delete v1 和增量接口数组仅静态；Guest.create
v1 及 unmounted 空槽也有静态证据。Apple 共享网络/创建版本及空槽已修源码并补
回归，但无 Swift 工具链，未编译、未出 Mac 包。Windows 网络管理仍待依此实施；
iPhone/iPad 不扩写，Android 不改，不提升任何真实写兼容等级。

2026-09-20 Windows 传输通知已补应用级监听/退出注销、连接级上下文及排队后复核，
显示失败不重复注册，销毁后不复活，旧通知不跳转其他 NAS。新增 16 项核心测试，
全量 3396 通过；原生双语各 8 场景使用真实 SDK Builder 和合成注册后端验证路由。
真实通知中心、系统权限及冷启动仍待用户验证；不恢复跨重启的内存传输历史。

2026-09-20 Windows 设置已恢复云盘管理入口和返回路由，复用原映射/缓存/离线服务。
默认关闭系统注册，用户可在云盘页明确确认本次启动的只读测试，重启需再确认；
已有映射可能恢复连接，不自动新增映射。新增登录启动选择默认不选，旧映射字段
不迁移。双语各 9 原生场景通过，全量 3380 通过；真实注册、打开/离线、外接卷及
重启未验证，不把入口恢复称为系统行为全部验收。

2026-09-20 Windows 托盘已补 Shell 重建恢复、版本 4 键盘/图标身份检查、非托盘
菜单命令隔离与销毁防复活；图标不可用时关闭只最小化，不隐藏返回入口。双语原生
各 7 场景通过，全量 3379 通过；真实 Explorer 重启仍待用户验证，未重启系统进程。
云盘入口与通知生命周期已由后续独立波次补齐，系统行为和全入口最终对账仍独立记录。

2026-09-20 Windows 控制台已接独立窗口、全屏和每窗口临时浏览会话，沿用新鲜状态/
布局核查、同源地址与既有证书读取。真实 WebView2 合成回归已验证 InPrivate、Cookie
属性、CSP、外部导航/弹窗阻断、关闭取消与目录清理；没有连接真实 VNC。系统信任
连接按能力可进入，固定指纹模式仍不释放网页凭据，不以错误事件代替完整证书策略。

2026-09-20 Windows CPU 优先级五档已接既有 VMM 编辑窗口，读取失败仅影响该字段，
与其他设置一起改动时一次内部 set v1、同源 get v2 核查；普通设置保持公开接口。
原确认/未知恢复和模块互锁复用，新创建请求不能携带此编辑专用字段。全量 3322
通过，原生与双架构交付继续核对；没有真实优先级保存，其他四端本波未改。

2026-09-20 官方 VMM 只读观察确认版本/能力和设置读取，静态脚本指出启动三态与
五档 CPU 权重。Apple 源码已修 Mac 开关误发“恢复原状态”、缺高优先级及 set
跟随读取版本的问题，移动端仅纠正只读三态显示。新增 Swift 回归尚未执行，没有
Mac 包或真实保存；Windows 内部优先级/高级设置/控制台仍需推进，Android 未改。

2026-09-20 Windows 创建后开机已接可选确认、完整资源/配置核对后单次公开 poweron、
只读恢复和失败保留 VM；默认不选，恢复后的新增写须再次确认，未知不重放。复用
原电源提交和核查，不扩展公开 create 字段；来源未核查的映像盘不自动开机。
新增后端/状态回归已通过，双架构 Release 包内全量 3299 通过，原生向导覆盖确认、
恢复与未知反馈；认证失效停止自动核查。真实 VM 行为待用户验证。

2026-09-20 Apple 公开 VMM 电源源码已改为 v1 单目标请求、完整预检与状态核查；
未知同操作只核对不重发，停止后续项，确认后选择变化不提交。公开 reboot 无契约，
不猜方法或代做强制开关机。新增共享 11 / Mac 模型 2 项测试未运行（本机无 Swift），
没有 Mac 新包；内部电源分支/Windows/Android 不改，iPhone/iPad 不扩电源入口。

2026-09-20 Windows VMM 映像删除已接公开 v1 单 ID、完整确认、逐项结果及只读核对，
与 VM 目标显式区分；不依赖无关 Guest API，创建源映像双向互锁。新增 19 项测试，
全量 3286 通过；真实 disk/ISO/Virtual DSM 删除与占用行为待用户验证，其他端未改。

2026-09-20 Windows VMM 已结束任务清理已接确认、冻结摘要范围、单 ID clear、严格
终态/回读与未知只读恢复；创建核查仍引用的任务记录受保护。原始 ID 不进 UI，
不删除 VM/映像、不停止任务。新增 14 项测试，全量 3267 通过；实际 NAS 待验。

2026-09-20 Windows VMM 删除已接单项/多项确认及逐台公共 Guest.delete v1，要求
已关机、身份/名称一致及严格同源列表回读。未知与电源/设置/创建互锁，不重发，
只读核对不继续剩余项；删除与批量电源复用呈现和批次状态，危险确认独立。新增
22 项测试，全量 3253 通过；真实 VM 删除未验证，其他四端本波无源码变化。

2026-09-20 Apple 共享 VMM 公开删除源码修正为固定 v1 单 ID、空成功响应和严格同源
列表回读；未知重复调用只核对、不重发，取消及部分结果不继续剩余项。移除映像
删除虚构的任务轮询，新增 11 项 Swift 测试但未运行（本机无 Swift）。未生成 Mac
修复包，iPhone/iPad 删除范围不扩展，Windows/Android 源码不改；目标构建及 NAS 待验。

2026-09-20 Windows VMM 多台开机/正常关机/强制关机已接原生确认和完整集合预检，
逐台使用原公共电源接口，未知/取消/认证失败停止；核对只读且不继续未开始项。
接受后的已知电源过渡持续等待目标状态、可取消，丢失回执仍未知不重发。新增 18
项测试，全量 3231 通过；其他四端不改，真实 VM 电源副作用仍待用户验证。

2026-09-20 Windows 回收/恢复已有独立只读核对入口，与复制/移动共用呈现和状态，
分别保持路径/时间和 blocker 约束。来源变化不再视为消失，成功证据保留至本地
回执；提交前失败不产生未知，健康回收任务持续等待且可取消。官方 total=-1 /
processed_num 校验已修正，其他四端不改，实际 NAS 回收/恢复仍待用户验证。

2026-09-20 Windows 复制/移动新增当前连接内的只读核对列表和窗口。稳定身份绑定
已有未知任务，不走启动接口，成功证据保留至界面接收；关闭/重开、取消、会话隔离
及精确匹配解除 blocker 已实现。提交前认证/网络失败不再误标未知；其他四端与
持久化结构不变。重登/重启不保证保留清单，真实 NAS 仍待用户验证。

2026-09-20 Windows 文件页单项/多项复制移动已接默认跳过同名与主动覆盖确认，跳过
独立计数；覆盖须任务完成与回读联合证据，禁止以旧文件存在判断成功，不提供覆盖
撤销。目录子项互斥、可取消的持续轮询及 total=-1 已补。新增 25 项测试，全量
3164 通过；两条公开覆盖 fixture 完成，其他四端不改，真实 NAS 覆盖未验证。

2026-09-20 Windows 文件页回收/恢复已取消 20 项上限，完整不可变快照、原生选择
复核、默认取消、实时进度与主题跟随均已接入。未知/取消不重发；两类认证失败均
停止并提示登录，复制/移动同期补认证异常提示。恢复不覆盖；Mac 恢复基线为单项，
Windows 批量属于既有扩展。其他四端及旧照片兼容入口不改，真实 NAS 待用户验证。

2026-09-20 Windows 文件页复制/移动已解除 20 项限制，使用完整不可变源快照和祖先
路径查表；确认时核对原生选择与全部源，未知/取消继续阻断，认证失效停止后续请求。
补实时进度与弹窗深色主题，新增 9 项单测，全量 3125 通过，双语各 13 原生场景通过。
旧照片兼容 UI、回收及其他四端不改；同名覆盖等剩余语义不因本波规模修复宣称完成。

2026-09-20 Windows 普通多文件上传已移除选择器/拖放的 20 项上限，完整快照串行
执行，选择器返回后核对目标与覆盖选择。全量 3116 通过，双语各 12 原生场景通过；
保留默认不覆盖、目标互斥、取消和逐项结果。Mac 基线为多选普通文件而非目录，
本条普通文件波次未改文件夹上传；后续独立波次已移除目录上传 20 文件/20 目录/
8 层限制，增加后台可取消规划/复查、完整确认和父链联接检查。全量 3374 通过，
双语各 12 原生场景及普通上传中文 12 场景通过；不覆盖和未知目录停止保持。
独立包为 20260920-174520，其他四端不改，真实 NAS 待用户验证。

2026-09-20 Windows 文件多选下载已对齐 Mac 的主流程：单文件原样保存，单目录或
多个文件/目录通过公开 Download v2 一次 path 数组下载 ZIP；文件页不再限 20 项。
保留事务式保存、取消及本地目标互斥，选择器返回后核对完整快照。新增 26 项测试，
全量 3110 通过，双语原生各 12 场景通过。Photos 逐项保存和其他批次限制未改，其他
四端实现不改；真实 NAS 的 ZIP 内容、权限及 URL/容量边界仍待用户验证。

2026-09-20 Mac 挂载恢复已接共享核心与原生窗口源码：操作编号和无密码配置只存本次
Repository 内，未知锁住关联路径并只读核查，待继续需重新确认，允许明确结束未执行
步骤但不回滚或遗忘未知。提交后取消、第二步失败与外部旧连接消失均保留阶段语义；
新增 15 个 Swift/Mac 方法未运行，本机缺 Swift，目标构建/原生验收未完成。其他四端
实现不改；Mac 重连/退出后的自动恢复不作已实现承诺，用户需先核对 NAS 状态。

2026-09-20 Mac 挂载确认前绑定：窗口先读并显示完整来源，提交传递同一身份；仓库
预检当前身份/目标目录/嵌套占用，换位置修改在断开前再次核对，结束时重查新连接。
旧无身份签名不提交；确认绑定旧身份及新配置，actor 内保护并发父子路径。新增
14 个 Swift/Mac 测试方法未运行，无 Swift 的本机只完成源码/静态门；未知恢复仍待
实施。其他 Apple 端使用兼容默认接口，不新增入口；Windows/Android 本波实现不改。

2026-09-20 Mac 挂载写后证明已补共享源码：严格 Mount.List.get 身份清单与公开目录
类型一起核对来源、协议、位置及自动挂载标志；新位置身份不符不进入旧位置卸载。
断开须清单消失与目录状态同时成立，读取失败不作消失。新增 7 个 Swift 测试方法
未运行，本机缺 Swift，仍未构建 Mac；确认前旧连接绑定与未知结果恢复尚待实施。
五端仅 Apple 共享读取兼容增量，移动端不加入口，Android/Windows 本波不改。

2026-09-20 Mac 挂载同类缺陷：共享源码纠正 mount_remote 七字段、域账号和 NFS 版本/
传输选项，断开使用 Mount.List.unmount 数组；编辑器移除无证据的只读保证并绑定
配置确认、清理密码。8 个新 Swift 测试方法（含三份 fixture 调用链）未运行，本机
无 Swift/Mac，不表述为目标构建通过。iPhone/iPad 只受共享构造器兼容回归影响，不
新增入口；Android/Windows 实现不改。Mac 源身份核查与恢复仍是源码待办。

2026-09-20 重启过渡态：Windows 追加 Restarting 值并显示双语状态，运行分组和
停止/重启选择包含该状态；Mac 同步控制预检及状态显示，启动/删除不因过渡态放开。
操作完成仍要求退出过渡态，重启仍核对同一实例启动时间变化。只修改 Windows 与
Apple 共享/Mac 范围，移动只读入口与 Android 不新增能力；Swift/Mac 尚未编译验证。

2026-09-20 容器生命周期：Apple 共享适配器修正 name/force/preserve_profile，
Mac 确认绑定选择并按状态/启动时间核查；Windows 四操作及会话内恢复已接原生窗口，
包括多选确认、逐项反馈、未确认中止后续及只读核查。两端按官方静态规则排除容器
本身及所属项目的套件托管实例，Windows 双语合成验证通过，不等于真实写行为验收。
共享 fixture 已纠正，Android 容器写仍关闭且零写测试只读取该 fixture，无需改源；
iPhone/iPad 仍只读，须共享包回归但不新增写入口。Swift/Mac 构建和真实行为未验证。

最新 Windows 解压已接完整归档分页/子目录、保留目录或展开文件、同名子文件夹和
独立覆盖确认；RAR/TAR/GZ/TGZ/TBZ/BZ2/ISO 与 ZIP/7z 入口按 macOS 扩展名对齐，
实际格式仍须 NAS list 预检。预检/回读完整目标树和已知文件大小，不以顶层目录
存在证明内部文件完成。目标固定当前文件夹与 macOS prepareExtraction 一致，
“另选目标目录”不是 macOS 当前入口的缺口。其他四端不改，真实压缩包行为待用户验证。

2026-09-19 Windows 解压密码、自动/手选文件名编码及原地密码纠错已接公开 Extract v2；
list/start 保持同一选择，密码不保存在恢复状态。任务 finished 和输出回读共同确认
成功，41 项聚焦合成回归及双语各 8 场景通过，x64/ARM64 正式构建通过。
macOS/iPhone/iPad/Android 不改；此段为前一阶段证据，完整目录与覆盖的当前状态
以上方最新段落为准，真实解压仍待用户验证。

2026-09-19 Windows 高级压缩已接 ZIP/7z、四档级别和可选密码，沿用官方 Compress v3、
目录权限/冲突拒绝/任务核查。只扩展 Windows 的领域选项与兼容传输重载，不改变
macOS/iPhone/iPad/Android 的既有压缩调用、存储或权限。密码不进入日志/恢复对象；
33 项聚焦合成回归通过；修正任务未完成/失败但文件已出现时误报成功的问题，必须
任务完成且文件回读通过，回执丢失仅保持待核查。真实加密包可用性待用户验证，
2026-09-20 已移除 Windows 压缩的 20 项客户端选择上限，保持同目录一次任务及完整
预检/回查；跨分页大批量有合成证据，不宣称 NAS 请求大小或容量无限制。Mac 压缩
入口也固定当前目录，不另造跨目录缺口，其他批量操作限制本波不变。

2026-09-20 Windows 收藏已接 Favorite v2 的专用发送、完整列表预检/回查和文件页
添加/移除/核查入口，未知不重发，按 profile/会话隔离；真实 NAS 未实测。挂载
已纠正请求并接创建/断开/两种修改顺序的阶段式核心，39 项合成回归通过；未知不重发，
第二步明确继续前重查新旧连接，不盲目回滚或删目录。新原生管理窗口已接创建/修改/
断开与无密码恢复，按接口/会话/确认开放，不使用旧固定关闭标志；关闭或切换 profile
取消并清理密码。Apple/iPhone/iPad/Android 不改；真实挂载行为等级不提升。

2026-09-19 macOS 1.0.9 用户故障反馈：共享文件下载/照片保存改用应用临时区与系统
协调导出，Mac 照片保存明确持有位置授权；删除最小身份预检不依赖 EXIF 等详情。
iPhone/iPad/File Provider 共享层需回归，Windows/Android 本次不改。无 Swift 的
Windows 主机只完成源码/静态门，Mac 测试、沙盒及真实 NAS 未验证，不表述为已修复
用户全部故障。完整范围与风险见[照片计划](../development/NATIVE_DSM_PHOTOS_DEVELOPMENT_PLAN_ZH.md)。

本矩阵分开记录产品范围和验证证据。范围不是完成度，源码或自动化也不等于真实设备或
发布通过。当前状态见[当前开发进度](STATUS.md)，未来优先级见[产品路线图](ROADMAP.md)。

Windows 2026-09-16 原生视觉重建及本地运行证据见[重建账本](../development/WINDOWS_NATIVE_REBUILD_ZH.md)。
文件、登录、设备侧栏与 NAS 存储概览已重排；其他管理页和私有写能力不能据此标为完整 macOS 对齐。
照片时间边界、跨模块会话传递和 Container 读取偏差的修复及剩余业务缺口，见
[Windows 请求一致性复核](../development/WINDOWS_API_PARITY_AUDIT_ZH.md)；未提升任何真实 NAS 证据等级。

## 范围标签

Windows NAS 终端/代理已接原生表单、校验/确认、基线绑定保存和逐字段结果核对；有
源码与合成证据，但生产行为门仍关闭，不能据此标为已实机验收或完整 NAS 管理对齐。
Apple/Android 网络契约不变；Windows 仅增加向后兼容的请求和可选接口。

Windows 文件服务已接六组读取、原生表单与基线保存核心，保留旧属性并以字段位图控制
显示，FTPS 不冒充“仅加密”策略；完整预检、变化组提交和整体回读有合成证据，生产门关闭。

区域与时间：Windows 已接固定读取、NAS 墙上时间、原生编辑及分阶段 set/sync 核心，
源码/合成证据不等于真实改时验收，生产门关闭；Mac/iPhone/iPad 共享解码修复待 Mac
回归。网络契约不变，Android 代码未改，不将校时请求接受冒充时钟精度验证。

物理网卡：Windows 已有固定读取、部分失败快照、原生编辑及单网卡保存/重连核对核心；
未知字段不补默认值，新地址不使用旧会话，生产行为门关闭，真实网络变更尚未验收。

安全防护：Windows 已有四分区读取、按网卡 DoS、基线保存和原生编辑；防火墙使用配置档
任务、完成后清理与整体回读。假成功路径已移除，生产门关闭，不能外推真实防护验收。

硬件/UPS：Windows 六组保存、LED 设置/更新、设备范围/字段名/可信空值及原生编辑已有
源码和合成证据；生产门关闭，未知 LED 阶段不重发，不能外推真实风扇/休眠/UPS 验收。

DDNS：Windows 固定读取、四操作核心及原生编辑/确认/持续反馈/只读恢复已有合成证据；
生产门关闭、真实写验收待完成。Apple 适配器及 Mac 界面层不再用旧记录覆盖拒绝或
未知结果，Mac 回归待验，不把源码/合成闭环计为真实 DDNS 管理验收。

Windows 套件严格读取、三操作核心、原生确认/持续反馈/恢复与升级只读提示已有合成
证据；写门关闭，真实行为未验证。Mac 列表失败关闭修正待共享回归；Android 本波未改。

Windows 账号/群组严格读取及新建/编辑/删除/恢复核心和原生管理已有合成证据；
生产写门关闭。Mac 保存预检/真实回读与失败关闭修正待共享/App 回归，未做真实账号变更。

Windows 电源确认与不重放恢复已有合成证据，接受不等于设备动作完成；新会话和
人工设备核对后才解除阻止，生产写门关闭，真实电源仍待验收。
Windows 连接严格读取/完整目标断开/原始标识回读及原生确认/恢复已有合成证据；
Mac 同类修正待共享与 App 回归，生产写门关闭且未验证真实断开。

Windows 任务计划 v3/v4/v1 读取、v3 命令和 v4 双基线保存核心已有合成证据；原生
管理已接列表/编辑/四命令确认及按需记录输出，合成场景通过、生产门关闭。Mac 保存/新回读
修正待回归，不代表真实计划生效。

Windows 硬盘检测稳定目标、状态和最近历史 v1 读取已有合成证据，旧猜测写入口关闭；
正确启停/有限核查/不重放恢复及原生面板已有合成证据，运行中只读刷新、写门关闭；Apple
严格读取、缓存绑定和未知反馈修正待 Mac 回归，不代表真实检测已验收。

Windows 远程访问 v3/v1 读取、受保护保存及原生确认/恢复表单已有合成证据，写门关闭；
Apple 严格独立读取和拒绝/未知结果修正待 Mac 回归，未执行真实中继或路由器变更。

Windows 电源计划与 USB/eSATA 外接存储已有只读原生列表/筛选/失败降级合成证据；
接口仍为 static 候选，没有计划保存或设备弹出，Apple 解析修正待 Mac 回归。

Windows 内存压缩已有 v1 只读摘要和原生状态展示，未知与关闭/零容量区分；仍为
static 候选，不开放设置，Apple 严格字段修正待 Mac 回归。

Windows 容器网络只读详情和原生展开已有合成证据，关联为空与名称未知分开；
网络创建/删除核心及原生表单已有受保护提交/回读合成证据；删除保护系统/占用网络，
多选变更使确认失效，未知结果只核查。其他容器/VMM 高级管理仍未完成，写门关闭，
Apple 严格计数修正待回归。

2026-09-20 Windows 镜像删除已接标签级身份、多选确认、完整清单/容器占用预检、
仓库标签数组或裸 identity 提交与结果回读；部分/未知只核查不重放。接口能力满足时
允许用户主动验证，无真实删除证据；Mac 单 id、多标签及只读核查已修源码，新增
Swift 回归未运行、未出 Mac 包。Mac 拉取追踪已补源码；移动端和 Android 不开放写入。

Windows 映像仓库已接 Registry.search/tags v1、首批结果与标签筛选/选择原生流程，
并新增按任务身份跟踪下载、确认、进度、可见窗口 5 秒刷新和只读恢复；缺回执或
结束状态不明不重发、不报完成。关闭不取消 NAS 下载。Mac 同类追踪已补源码但未构建；
真实完成/失败字段未实测，不升级套件兼容证据。
拉取必须显式确认，不产生隐式写请求；Apple 共享解析和任务追踪仍待 Mac 目标回归。
官方 Image.list 完整分页参数已补 Windows/macOS 读取，并拒绝不完整目录；下载任务
接口和多标签模型的兼容扩展现已获用户批准；真实写行为仍单独验收，不改变其他端入口策略。

Windows VMM 既有七分区完整解析返回数组，不再截断 200 项；内部日志 JSON 请求和
能力校验已修，只有合成证据。日志保留既有首批范围，没有新增写或内部回退。

后续已新增官方 v1 单机电源核心及原生确认/核查：三个动作分别处理，按 guest_id 提交
一次并由 Guest.get 确认状态，不虚构任务 ID；生产门未开放，真实写待验收。基础编辑已接
公开 Guest.get/set v1 的名称、说明、CPU、内存和三态自动启动；仅发送修改字段，原值
漂移拒绝保存，逐字段核查，未知与电源操作互锁。Windows 有自动化/双语原生合成证据，
保存门关闭；macOS/iPhone/iPad/Android 代码不改，不外推各端内部接口语义。创建、高级
编辑、批量/重启和控制台仍属于后续主线，不以本切片代表完整 VMM 完成。

2026-09-19 Windows 增量接入官方 Task.Info v1 list/get 与独立任务页：完整清单、逐项
失败隔离、敏感 ID 摘要、可见且未确认结束时自动刷新；结束不作创建成功。其他四端
接口/入口不变，未清理真实任务或新增内部请求。创建和映像导入仍需具体身份与资源
回读，任务页面不能替代相应功能闭环；真实 NAS 与辅助功能验证待用户执行。

后续 Windows 增加公开创建核心及会话内恢复：空白/映像盘、连接/未连接网卡，绑定
本次任务及新 VM，基础设置分步一次性提交，失败不自动删除。已有 HTTP 合成回归，
但原生向导未接、生产门关闭；克隆来源无法由公开字段完整证明。其他四端不改，
公开 Storage.list size/used 单位修正仅作用于 Windows。核心构建不代表创建用户
流程完成，真实 NAS 副作用及资源上限仍未验证。

最新状态（2026-09-19）：创建原生向导、恢复和明确继续已接；VMM 电源、基础设置、
创建的固定关闭常量已按用户授权移除，改以当前会话与公开能力判断，所有确认/
预检/重复保护及结果核查仍执行。公开入口与双语原生合成回归已有证据，不等于真实
NAS 验证；前述“向导未接/门关闭”为早期阶段记录，不再代表当前 VMM 源码状态。
NAS 专用管理、容器网络及 Chat 高级功能也已改按接口/会话/权限开放；Chat 不再要求
读取系统套件管理摘要。当前只有合成证据，未实现的操作不因此开放；其他四端不改。

随后在用户授权的同类缺陷范围内，macOS 共享 updateVirtualMachine 补齐六类发送字段
的内部清单精确回读，不使用展示默认值或公开清单替代内部设置证据。4 个新增 Swift
测试未执行（Windows 主机无 Swift）；未改变方法签名、存储或其他端入口。内部
autorun 的用户语义及真实副作用仍未验证，不能据此宣称整个 macOS 编辑完成。

Windows 传输中心隐藏/关闭与首次读取等待已分离；活动刷新器销毁即时关闭新请求，
等待旧/新代次停止完成，迟到结果不应用。仅本机合成验证，不改变后台注册或 NAS 任务。
两来源已有逐页加载、同游标失败重试和展开范围刷新；原生列表增量更新，未变更行对象
保留。沿用既有分页接口，无公开契约/存储迁移，真实长列表与网络抖动仍待验收。

Windows 拖放移动及撤销复用现有批量确认/回读，来源用一次性票据绑定，目标权限重新
读取；部分/未知不显示全部可撤销。命名空间资源键按实际 PRI 段转换，普通键不变。
仅源码/合成证据，不增加真实 NAS 写验收或改变其他端接口。

Windows 任务文件创建已接入目录/密码选项；Windows 与 Apple 文件上传按目录参数选择
Task v1/v2，密码不因规范化丢失空白。源码/合成证据不替代真实上传、解压和 Mac 构建。
Windows Download Station 设置已有 Info v2 默认位置、v1 基础降级、原生目录选择及分组件保存/
核对；仅增加源码和本地合成证据，不代表 RSS、批量、删除数据或真实 NAS 管理全部完成。
范围与命令见[Windows 账本](../development/WINDOWS_MACOS_PARITY_DEVELOPMENT_PLAN_ZH.md)。
Windows 任务批量操作已有暂停/继续/移除和明确的结束未完成下载入口，未知只核对、不重放；
结束任务不等于删除数据，真实目录副作用仍待验收，不能据此标记文件删除已完成。

Windows Chat 在用户批准接口增量后新增提醒、纯文字定时、无附件投票、批量关闭/转发/本人消息删除、新联系人转发及群公告设置/取消；
新写受已知 DSM/Chat 组合、逐项能力、确认和回读保护，未知版本只读。当前仅源码/本地合成
证据，真实行为与其他平台不据此升级，见[高级动作记录](../api/discovery/endpoints/chat-advanced-actions.md)。
Windows 实时订阅已贯通原生 WebSocket、现有证书管线、事件合并回读及可见性取消；连接失败
继续轮询，真实 NAS/中继/睡眠验收仍待完成，见[实时记录](../api/discovery/endpoints/chat-realtime.md)。

照片范围统一见 [Synology Photos 计划](../development/NATIVE_DSM_PHOTOS_DEVELOPMENT_PLAN_ZH.md)：五端正式入口已切换为个人照片时间线、搜索与 12 类筛选、相册／分类、共享条目读取及媒体预览／原件保存；个人单项删除按授权依接口、权限和确认开放。共享照片空间与其他写操作仍关闭。旧文件导入／备份通用代码不再充当 Photos 浏览降级。范围与源码迁移不等于真实 NAS 或发布验收。

| 标签 | 含义 |
| --- | --- |
| 核心 | 当前平台应交付的用户主流程；必须有对应安全边界与自动化。 |
| 受限 | 当前只交付明确列出的安全子集；未列出的写入、后台或系统集成继续关闭。 |
| 后续 | 有产品价值，但不进入当前实现 DAG 或 `PENDING_USER_VALIDATION`。 |
| 非目标 | 当前平台不做；不得因其他平台已有实现而自动迁入。 |

## 证据维度

| 维度 | 含义 |
| --- | --- |
| 源码 | 当前仓库中存在对应实现与明确安全边界。 |
| 自动化 | 当前平台或共享层有可重跑的测试、构建或静态门禁。 |
| 真机 | 在真实目标设备、受控账户和必要 NAS 环境完成脱敏验收。 |
| 发布 | 已满足目标平台分发、签名、安装、回滚和发布策略。 |

`未验证` 仅表示缺少该维度的证据；不能由相邻平台、其他 DSM build 或模拟器结果推断。

## 平台级证据状态

| 平台 | 源码 | 自动化 | 真机 | 发布 |
| --- | --- | --- | --- | --- |
| macOS | 已建立核心客户端和桌面挂载；指定文件夹可确认启用写回，默认只读，Finder 删除关闭。 | 共享 Package、写回回归和无签名构建已通过；云端门禁可重跑。 | 旧版升级与只读挂载已有用户反馈；本次写回、不同架构及真实 NAS 异常场景待验证。 | 已建立签名、公证与正式更新流程，发布结果见[macOS 发布指南](../releases/MACOS_GITHUB_RELEASE_ZH.md)。 |
| iPhone | 已建立移动核心与受限能力。 | Apple Build 包含共享 Package、通用构建及独立 iPhone 模拟器合成回归；提交结果见 Checks。 | 未验证：真机、系统选择器、网络、VoiceOver 与真实 NAS。 | 未发布：等待 Apple Beta 决策。 |
| iPad | 已建立与 iPhone 共用的移动核心和宽屏路径。 | Apple Build 独立执行 iPad 模拟器合成回归并保留结果；提交结果见 Checks，不借用 iPhone 结论。 | 未验证：分栏、键盘、多任务、VoiceOver 与真实 NAS。 | 未发布：不以 iPhone 结果替代。 |
| Android | 已建立 Compose 客户端、后台任务和质量门。 | 单元、增量构建与 JSON 质量基线可重跑；完整门禁由托管 Runner 执行。 | 未验证：真实设备、证书、后台、危险写和 NAS。 | 未发布：完整构建与设备验收分开记录。 |
| Windows | 已建立 WinUI、领域与基础设施路径；系统集成写操作保持关闭或只读。 | xUnit、XAML 和目标架构构建由 Windows Runner 执行。 | 未验证：Explorer、Cloud Files、通知、安装和真实 NAS。 | 未发布：不改变当前发布形态。 |

## SEC-002：File Station 上传认证位置

此项是 macOS 发布整改的受限安全收敛，不代表五端都已完成同一迁移。共享
`file-station.upload.synthetic-overwrite` Fixture 仍记录 Android 的旧 URL 认证位置；它不能
降低 Apple 的 URL 凭据禁止要求。

| 平台 | 当前证据与范围 |
| --- | --- |
| macOS | Apple 共享网络层已禁止文件读取和上传 URL 携带会话或 Token，并有合成请求测试；真实 DSM、重定向和发布环境仍待验收。 |
| iPhone / iPad | 与 macOS 复用 Apple 网络层；本轮没有运行单独移动构建或真机验证。 |
| Android | File Station 上传仍使用 URL 认证字段，测试也明确记录该现状；迁移需要独立授权、契约更新和 Android 门禁。 |
| Windows | File Station 上传 URI 已不含会话或 Token，使用 Cookie、Header 和 multipart；本轮只读核实，未执行 Windows 发布验收。 |

## 用户能力范围

| 能力 | macOS | iPhone | iPad | Android | Windows |
| --- | --- | --- | --- | --- |
| 登录、会话、安全存储 | 核心 | 核心 | 核心 | 核心 | 核心 |
| 多 NAS、QuickConnect 与证书确认 | 核心 | 受限 | 受限 | 核心 | 受限 |
| 文件浏览、搜索与预览 | 核心 | 核心 | 核心 | 核心 | 核心 |
| 文件上传、下载与前台传输 | 核心 | 受限 | 受限 | 核心 | 受限 |
| 文件复制、移动、回收站与分享 | 核心 | 受限 | 受限 | 核心 | 受限 |
| 文本编辑与复杂批量管理 | 核心 | 非目标 | 非目标 | 受限 | 受限 |
| Photos 浏览、原件保存／系统分享与个人单项删除 | 核心 | 核心 | 核心 | 核心 | 核心 |
| 自动照片备份 | 后续 | 后续 | 后续 | 受限 | 非目标 |
| Chat 会话与文字消息 | 核心 | 受限 | 受限 | 核心 | 受限 |
| Chat 附件、提醒、定时与投票 | 核心 | 受限 | 受限 | 核心 | 受限 |
| Chat 加密、语音与实时通话 | 后续 | 非目标 | 非目标 | 后续 | 非目标 |
| Download Station 基础任务 | 核心 | 受限 | 受限 | 核心 | 受限 |
| Download Station 设置、RSS、批量与删除数据 | 受限 | 非目标 | 非目标 | 受限 | 受限 |
| Container Manager / VMM 只读摘要 | 核心 | 受限 | 受限 | 受限 | 受限 |
| Container / VMM 生命周期、删除与控制台 | 受限 | 非目标 | 非目标 | 受限 | 受限 |
| NAS 健康、存储与服务只读摘要 | 核心 | 受限 | 受限 | 核心 | 受限 |
| NAS 账户、网络、套件、电源与磁盘写入 | 受限 | 非目标 | 非目标 | 受限 | 受限 |
| Desktop Cloud Drive / File Provider / Cloud Files | 核心 | 非目标 | 非目标 | 非目标 | 受限 |
| 常驻后台传输、系统通知与系统级集成 | 受限 | 后续 | 后续 | 受限 | 受限 |

## 安全开放规则

上述 Windows“受限”不是未实现或全部关闭：Chat 已接附件、提醒、纯文字定时及无附件
投票；Download Station 已接设置、批次和已记录删除语义，不据此声称 RSS 已实现。
Container/VMM 和 NAS 专用操作范围以对应账本为准；云盘源码缺口已按上文最新复核补齐，
目标设备/真实 NAS 及当前 Windows 云端门禁仍分别记录。

2026-09-19 用户验证策略补充：Windows 已实现的 NAS 专用管理与容器网络创建/删除
不再按固定 DSM build 或行为验收常量关闭。NAS 系统写入要求当前明确管理员、
有效接口及每次确认/预检/防重复/回读；容器按套件权限处理，不强加 DSM 管理员身份。
旧无确认通用辅助、缺生产传输或未实现的功能不在开放范围。只有合成/构建证据，
不提升真实验证等级；其他四端策略和实现不因本次 Windows 调整改变。

| 范围 | 必要条件 | 未满足时的行为 |
| --- | --- | --- |
| 公开写 API | 版本化契约、权限检查、确认、重复提交保护和结果回读。 | 拒绝提交并提供恢复路径。 |
| 内部只读 API | 私有 API 记录、能力探测、可失败降级和脱敏 fixture。 | 不阻断无关主流程。 |
| 内部写 API | 已记录 DSM/套件版本、专用环境行为验证和最终状态复查。Photos 个人单项删除按明确授权豁免版本白名单，仍须能力／权限／确认／防重复／回读。 | 无相应授权或条件不足时关闭；未测版本不标为已验证。 |
| 认证与证书 | 平台安全存储、会话隔离和用户可理解的确认流程。 | 不显示秘密，不绕过证书校验。 |
| 后台、跨 NAS、File Provider / Cloud Files | 唯一所有者、取消语义、恢复策略和平台系统验收。 | 保持前台、只读或能力门保护。 |

## 取舍说明

- macOS 是业务语义与安全行为基准，不是 iPhone、iPad 或 Windows 的逐像素模板。
- iPhone 和 iPad 只实现本矩阵中的核心或受限结果；桌面悬停、右键、菜单栏和常驻进程不
  因共享代码存在而进入移动范围。
- Android 的功能范围由 Android 专项计划单独维护；本矩阵只约束跨端安全语义和证据表述。
- Windows 以完整业务语义对齐为目标，但遵循 WinUI、键鼠、触控、窗口和资源管理器习惯。
- “后续”和“非目标”不应被写进发布待办或真机验收清单；它们需要单独产品与契约决策。

## 相关文档

- [Android 长期计划](../development/ANDROID_CLIENT_COMPLETION_PLAN_ZH.md)
- [Apple 移动端长期计划](../development/APPLE_MOBILE_MACOS_PARITY_DEVELOPMENT_PLAN_ZH.md)
- [Windows 长期计划](../development/WINDOWS_MACOS_PARITY_DEVELOPMENT_PLAN_ZH.md)
- [macOS 对齐总控计划](../development/MACOS_PARITY_REPLICATION_MASTER_PLAN_ZH.md)
- [DSM 兼容矩阵](../compatibility/DSM_COMPATIBILITY_MATRIX.md)
- [发布与手工验收历史](../archive/2026-h2/RELEASE_VALIDATION_HISTORY.md)
- [macOS 首个 Beta 就绪报告](../quality/MACOS_BETA_READINESS_ZH.md)

## 2026-09-26 NAS 设置读取回归与测试入口

| 平台 | 本次影响 | 验证边界 |
| --- | --- | --- |
| macOS | 修复电源计划/账号/连接读取；系统活动兼容；新建任务本地草稿；恢复入口按能力开放 | Swift 聚焦测试与本地构建，五个页面已实机读取，真实写入待用户测试 |
| iPhone | 共享 Apple 网络适配会继承向后兼容解析修复，不新增页面或移动能力 | 未执行 iOS 构建和真机验收 |
| iPad | 同 iPhone；不引入桌面电源计划保存/进程控制 | 未执行 iPad 构建和真机验收 |
| Android | 不修改源码；需在后续独立切片核对同类响应枚举与容器 | 未运行本次 Android 门禁 |
| Windows | 不修改源码；需在后续独立切片核对同类响应枚举与容器 | 未运行本次 Windows 门禁 |

没有修改公开方法签名或请求字段、持久化格式、正式签名权限和最低系统版本。
用户授权开放因未实测而关闭的已有功能；不把未实现能力伪装成可以提交的按钮，
不绕过明确的服务端权限拒绝。发现记录见
[只读观察及限制](../api/discovery/environments/2026-09-26-nas-settings-read-observation.md)。

2026-09-27 续验修复未登录空权限摘要误判及独立测试包退出被共享容器检查阻断；正式版待上传保护保留；最终包退出后重新连接已由用户确认菜单正常。
当前真实页面与登录回归范围见 [实测记录](../api/discovery/environments/2026-09-27-nas-settings-live-validation.md)。
