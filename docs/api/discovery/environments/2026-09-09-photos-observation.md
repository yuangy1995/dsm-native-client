# Synology Photos 只读发现环境快照

## 基本信息

| 字段 | 值 |
| --- | --- |
| 环境标识 | 待归属观察，不因版本相同认定为既有设备 |
| 匿名设备别名 | 待核实与既有 lab-a 的关系 |
| 基线状态 | 待核实，不新增 current 基线 |
| 替代的旧基线 | none |
| 发现日期 | 2026-09-09 |
| DSM 版本 / build / Update | 7.2.1 / 69057 / 12，官方初始化响应核实 |
| 设备架构类别 | 未验证 |
| 连接方式 | 未验证具体直连或中继类别 |
| 证书类别 | 未验证 |
| 账号权限类别 | DSM 管理员；Photos 共享空间权限为 none，不因管理员身份推断照片权限 |

## 相关套件

| 项目组件标识 | 显示名称 | 完整版本 | 运行状态 | 备注 |
| --- | --- | --- | --- | --- |
| synology-photos | Synology Photos | 1.8.2-10090 | 运行 | 本次 Setting.Admin.get 响应核实 |

## 证据来源

- 已登录 Chrome 官方 DSM 与 Photos 页面；通过官方页面重新加载和浏览切换观察请求，仅输出控制参数和响应字段类型。
- `SYNO.API.Info` 确认 Photos 相关声明使用 JSON 参数编码；Item 最高 v6，但官方网页列表实际调用 v4，不能直接改用最高版本。
- 端点记录：`docs/api/discovery/endpoints/photos-library-read.md`。
- 不保存原始网络导出、身份凭据或用户照片数据。

## 发现范围

- 目标：照片、时间线、文件夹、相册、媒体、搜索与可用权限的官方请求结构。
- 明确不检查：通过真实照片写入探测删除、移动、相册修改、分享或权限变更。
- 当前限制：读观察不等于写行为验证；未知版本写入口保持关闭。

## 安全检查

- 不保存 Cookie、SID、SynoToken、DID、OTP、密码、地址或账号。
- 不保存真实路径、照片、文件名、相册名、人物、地点或元数据正文。
- 只保留理解契约所需的结构与脱敏证据。

## 结论

- 已确认：个人空间时间线、项目列表、文件夹读取与部分分类列表的成功响应结构；详见端点记录。
- 未验证：共享空间成功读取、App 自身登录会话访问、写操作、其他套件版本。管理员账号不能替代普通用户覆盖。
- 本记录随观察更新；接口证据必须同步到端点记录和兼容索引。
- 后续只读补验：根目录 get(name="/") 成功，相册 list 成功但为空；照片／视频详情与视频流已观察。原件最小 Range 请求返回 206，核对响应头后立即取消正文，不保存真实媒体。具体参数和证据边界见端点补充。
