# 项目协作规则

- 使用中文编写项目文档和代码注释。
- 优先使用 Synology 官方公开 API，内部 API 必须明确标注。
- 不提交密码、OTP、SID、SynoToken、Cookie、DID、证书私钥或真实用户数据。
- 不提交未脱敏的 HAR、PCAP、DSM 响应、文件路径和主机地址。
- 临时调试代码、临时抓包、临时测试账号资料和生成物在任务结束前删除。
- 正式自动化测试属于项目源码，可以保留；一次性调试文件不得提交。
- 修改 API 契约时评估并同步受影响的五端实现计划和兼容矩阵。
- 危险写操作必须具备确认、权限检查、重复提交保护和结果校验。

## 多 AI 模型协作与任务交接

- 仓库源码、`AGENTS.md`、契约、ADR、平台矩阵和可复现的测试结果是事实来源；其他模型的描述、历史对话和任务总结只能作为线索，存在冲突时必须重新检查仓库与验证结果。
- 开始修改前必须阅读适用范围内的 `AGENTS.md`，检查 `git status`、相关差异和目标文件；工作区可能包含用户或其他模型尚未提交的改动，不得擅自覆盖、回退、格式化或删除。
- 多个模型并行工作时必须拆分清晰且互不重叠的文件或模块范围；不得同时修改同一文件。写入前再次检查目标文件差异，发现并发变化时先合并上下文，不得用旧版本覆盖新内容。
- 延续项目现有架构、命名、资源键和实现模式。未经明确论证，不得为同一能力新增平行实现、重复抽象、临时兼容层或仅供某个模型使用的专有流程。
- 不得把推测当作事实，不得虚构 API、系统行为、测试结果或实机结论。无法验证的内容必须明确标注为“未验证”，并说明所需平台、设备或条件。
- 修改范围应与当前任务一致；发现相邻问题可以记录，但不得借机进行无关重构、批量格式化、依赖升级或配置迁移。
- 避免过度防御性编程。新增校验、降级和兼容分支必须对应已记录契约、可复现故障或明确风险；不得为纯假设场景堆叠重复校验、平行 fallback、吞错或不可达分支。安全、隐私、危险写操作和数据完整性门禁不属于过度防御，不得以本条为由削弱。
- 开发默认以尽快形成可使用的完整功能闭环为优先：先实现主流程、必要错误处理和聚焦自动化，再处理不影响主流程的极端场景、体验微调和性能强化。不得为了预想中的罕见情况先扩张抽象、校验层或兼容分支，也不得因顺手完善非当前功能而拖延交付。
- 只有真机、正式签名、entitlement、系统注册、特定硬件或真实 NAS 环境才能完成的调试与验收，默认先跳过并后置给用户测试。Agent 应先完成源码、当前环境可执行的模拟器/构建、单元/集成/合成测试，并以 `PENDING_USER_VALIDATION` 记录前置条件、操作步骤、预期结果、需回传的脱敏失败信息和影响范围；该标签只是待办，不是验证等级。缺少真机本身不得把无依赖切片标为 `BLOCKED`，也不得把未运行的真机测试表述为通过。
- 若未完成真机验证可能造成数据丢失、凭据泄露、越权、危险写误触发或不可逆系统副作用，相应入口必须保持关闭、只读或受能力开关保护，直至用户验证完成；该限制只约束相关入口启用，不得借此暂停其他独立功能。用户返回真机结果后，优先修复可复现问题并补回归证据。
- 新增第三方依赖、改变工具链或最低系统版本、修改 Bundle ID、包名、签名、权限、数据格式、持久化结构或公开契约前，必须说明必要性、影响范围、迁移与回滚方案，并取得用户明确同意。
- 生成文件必须通过项目既定生成流程更新，不得直接手改；一次性脚本、日志、构建产物和模型生成的中间文件在任务结束前清理。
- 每次交付必须列出实际修改、关键决策、已运行的验证及未验证风险。测试必须报告真实命令和结果，不得以静态阅读、局部测试或其他平台通过代替目标平台构建与实机验收。
- 任务未完成而需要交给其他模型时，交接信息必须包含目标、已完成内容、改动文件、当前工作区状态、已运行验证、失败信息、剩余步骤和禁止触碰的用户改动，避免接手模型从头猜测或重复执行。
- 未经用户明确要求，不得执行 `git add`、提交、推送、创建或合并 PR，也不得改写提交历史；需要提交时只包含当前任务相关文件，并在操作前复核差异。
- Android 的完整 JVM、Debug/Release/R8、仪器测试 APK 与 lint 等高负载门禁优先交给 GitHub 托管 Runner；本机默认只运行增量编译、聚焦测试和无法由 CI 代替的设备调试，避免持续占用物理机 CPU。为触发云端编译测试，用户已授权在专用 `codex/` 功能分支创建并推送仅包含当前功能的临时验证提交，但不得推送凭据、本机配置、一次性产物或无关改动，也不得直接写入受保护分支。
- 同一完整功能点因 GitHub 测试修正产生多个临时提交时，功能验收后必须整理为一个语义完整的功能提交，再作为正式交付记录；不得把 `test`、`fix CI`、`retry` 等中间噪声永久留在正式历史。只允许整理当前专用功能分支，远端测试分支确需更新时使用 `--force-with-lease`，不得改写共享分支、他人提交或已合并历史。
- 用户明确要求功能完成后合并时，只有完整云端门禁通过并复核目标分支没有新的未整合提交，才能把已整理的单一功能提交合并并推送到 `main`；成功后删除对应远端和本地临时分支。云端失败、验证未完成或目标分支出现未知变化时不得强行合并。
- 向外部 AI、搜索、日志或问题追踪系统提供内容时遵循最小披露原则，继续执行本文件的秘密与隐私规则，不上传真实凭据、用户数据、未脱敏响应或私有环境信息。

## 跨端开发准则

- Windows 与 Apple 移动端对齐 macOS 时，先阅读 `docs/development/MACOS_PARITY_REPLICATION_MASTER_PLAN_ZH.md` 及目标平台专项计划；`apple/Apps/DsmMac/**` 是只读参考实现，Android 不属于这两条实施线，除非用户另行明确授权，不得修改二者。
- Windows 保持 macOS 业务语义完整对齐目标；Apple 移动端只实现专项计划中明确标为“核心”或“受限”的用户结果，不以 macOS 功能数量作为完成标准。标为“后续”或“当前不做”的桌面能力不得暗中进入实现 DAG，也不得写成 `PENDING_USER_VALIDATION`；必须记录取舍理由、移动替代路径，以及 iPhone 与 iPad 是否存在不同范围。
- 中大型任务必须完成基线确认、拆分、依赖排序、集成、复核和验收；不得仅依据历史总结或外部描述宣布完成，必须检查相关源码、差异和可复现结果。
- 每个开发波次开始前，当前任务负责人必须建立功能对齐账本，记录 macOS 证据路径、目标平台等价语义、交互转换、契约依赖、安全级别、当前验证等级和明确非目标；“页面存在”或“能够构建”不等于功能对齐。
- 跨端波次按“用户可完成的主流程 → 聚焦自动化与可用构建 → 后续用户真机验证”推进。真机、签名或真实 NAS 缺口以 `PENDING_USER_VALIDATION` 写入账本即可，不得阻塞不依赖它的下一个功能切片。
- 涉及共享热点文件时必须明确当前任务的单一修改范围。Shell/路由、组合根、共享领域模型、Repository 接口、工程文件、双语资源、契约、平台矩阵和进度文档属于集成热点，不得在同一波次中无边界地交叉修改。
- 功能实现同时负责本功能的新测试文件，但不得自行降低断言、删除既有测试或把真实环境缺失改成静默跳过。完成后必须报告实际改动、关键决策、测试命令与结果、未验证风险、工作区状态及建议的下一切片。
- 每个实现波次结束后必须执行一次独立集成审查；高风险写操作、认证/证书、后台任务、跨 NAS 传输、File Provider/Cloud Files 和私有 API 还必须进行只读对抗复核，再决定是否进入目标平台构建或实机验收。
- macOS 是业务语义与安全行为基准，不是逐像素模板。Windows 保持 WinUI/Fluent 的键鼠、触控、窗口和文件资源管理器习惯；iPhone/iPad 保持 SwiftUI、系统返回、触控、分享、Files/Photos、后台执行和多窗口习惯。任何桌面交互迁移到移动端时都必须记录“用户目标、移动端替代、不可用能力和降级”，不得照搬悬停、右键、双击、菜单栏或常驻进程。
- 若实现过程中发现 macOS 基线缺陷或必须修改 `apple/Apps/DsmMac/**`，停止该切片并记录证据，请用户决定是否解除只读范围；不得由对齐任务顺手修复。共享 Apple Package 只能做向后兼容的增量修改，并必须运行 macOS 回归测试，不能以“未改 macOS App 文件”推断 macOS 不受影响。
- 契约缺口不能通过复制 macOS 私有请求、解析 UI 文案或猜测字段补齐。需要新增或改变契约时单独建立契约切片，按五端影响规则更新；未获授权时只记录 Android 影响，不修改 Android 代码。

## DSM 与套件私有 API 发现

- 私有 API 发现必须遵循 `docs/api/discovery/README.md`。开始前先复制环境模板，记录 DSM 版本、build、Update、相关套件完整版本、日期、连接方式类别、权限类别和证据来源；不得只在对话中保留结论。
- 使用浏览器中已登录的 DSM 会话时默认只观察官方页面产生的必要请求。不得导出或提交原始 HAR，不得记录 Cookie、SID、SynoToken、DID、主机、账号、真实路径、消息、日志正文或其他用户数据。
- 发现阶段默认只读，不得为了探测而触发创建、修改、删除、启动、停止、安装、卸载、重启、关机、网络或权限变更。写接口只能在用户明确授权的专用测试环境中验证。
- 每个内部接口或不可拆分的紧密关联端点组必须建立稳定记录，并同步更新 `contracts/private-api/compatibility.json`；记录 API 名称、路径、方法、版本、参数与响应结构、权限、副作用、能力探测、失败语义、降级策略、证据等级和覆盖环境。
- 不同 NAS 使用不含设备信息的稳定别名 `lab-a`、`lab-b` 依次区分；同一 NAS 升级后沿用原别名并新建版本基线，旧基线改为 `historical` 且通过 `supersedes` 串联，不得覆盖历史记录或使用设备名、型号、序列号、地址作为标识。
- 证据严格区分 `static`、`observed`、`read-verified`、`behavior-verified` 和 `failed`。静态前端线索、观察到请求或其他版本结果不得表述为当前版本已经验证。
- 内部只读接口必须可失败降级，不得阻断无关主流程；内部写接口在未记录的新 DSM build 或套件版本上默认关闭，完成权限、重复提交保护和最终状态复查后才能形成兼容结论。
- DSM 或套件升级后必须保留旧记录、新建环境快照和升级报告，重新对比 API 路径、版本、必需参数、字段类型、错误码与权限，并同步更新端点文档、脱敏 fixture、Schema、自动化测试、五端适配评估和产品兼容矩阵。
- 发现产生的临时响应、截图、浏览器导出、代理配置和调试文件必须在完成脱敏记录后删除；只有彻底脱敏且对契约复验必要的 fixture 可以进入仓库。

## UI/UX Pro Max 自动使用

- 任何会改变界面外观、布局、组件、导航、交互、动效、可访问性或用户体验的任务，都必须自动使用 `ui-ux-pro-max` Skill，不需要用户重复点名。
- 开始 UI/UX 任务前，先运行 `python3 tools/codex/ensure_ui_ux_pro_max.py`；Windows 使用 `python tools\codex\ensure_ui_ux_pro_max.py`。
- 引导脚本仅在全局 Skill 缺失时安装固定版本。安装后必须完整读取 `$CODEX_HOME/skills/ui-ux-pro-max/SKILL.md`；未设置 `CODEX_HOME` 时使用 `~/.codex/skills/ui-ux-pro-max/SKILL.md`。
- 新页面或整体重设计先生成 design system；局部组件、体验审查和 UI 缺陷按 Skill 路由使用对应领域检索。
- Apple、Android、Windows 分别使用 `swiftui`、`jetpack-compose`、`winui` 技术栈指南，不接受 Skill 中与本项目实际技术栈冲突的默认值。
- UI 建议必须保持平台原生体验，并满足浅色/深色模式、键盘、触控、VoiceOver/屏幕阅读器、动态文字和降低动态效果要求。
- Skill 只用于开发期设计支持，不得加入 App 运行时依赖，不得借 UI 修改改变 API、安全策略、Bundle ID、包名、会话存储或业务语义。
- 纯网络、协议、存储、构建、CI 或其他不影响界面的任务不运行该引导脚本。
- macOS 工作区详情根视图必须占满 `NavigationSplitView` 的可用区域；带标题栏、筛选栏或列表的页面采用顶部对齐。
- SwiftUI 页面级内容统一使用 `fillsAvailableContentArea(alignment:)`；普通空状态居中，标题栏和筛选栏保持 `.topLeading`。
- 新页面必须分别检查加载、空内容、筛选后为空、错误和正常内容五种状态；空状态提供原因和下一步操作，弹窗状态分支占满标题栏以下的剩余区域。

## 面向用户的界面文案

- 所有面向终端用户的标题、字段、按钮、状态、提示和错误信息，默认按普通用户而不是开发者或协议调试人员的理解方式编写。
- 主流程不得直接暴露 API、HTTP 状态、能力发现、build、契约、Keychain、SID、SynoToken、证书钉扎等实现术语；确因安全核对需要展示指纹等技术信息时，必须先给出通俗解释，并将技术细节放在次级层级。
- 不得用“服务端处理、由 NAS 打包、递归、流式、分片、请求往返”等实现过程解释普通功能；默认只说明用户会得到什么、可能受到什么影响以及下一步怎么做。
- 错误信息必须说明“发生了什么”和“用户下一步可以怎么做”，不得只显示内部错误、错误码或无恢复路径的技术描述。
- 按钮名称应描述用户可预期的结果，例如“连接”“重新登录”“我已核对，继续连接”，避免使用内部动作名称。
- 新增或修改界面时必须审查全部可见文案；开发诊断信息只进入受控日志或可选的高级详情，不得混入默认用户界面。

## 国际化与本地化

- 所有新增或修改的用户可见文案必须通过平台语言资源提供，不得在 Swift、Kotlin、C#、SwiftUI、Compose 或 XAML 中新增硬编码显示文案。
- 初期支持英语与简体中文；每个用户界面资源键必须同时提供两种语言，缺少任一语言不得视为完成。
- 首次启动默认跟随系统首选语言；系统语言不属于英语或简体中文时必须回退英语。用户可固定选择英语或简体中文，选择结果在本机持久保存且不得与 NAS 配置、密码或会话绑定。
- 新功能必须同时覆盖标题、按钮、字段、状态、提示、错误、确认框、通知、菜单和无障碍文案，不需要用户再次提醒国际化要求。
- 翻译后的字符串不得参与业务判断、导航、筛选、持久化、API 参数或重复提交保护；领域层使用稳定枚举、错误类型或资源键，界面层负责本地化展示。
- 带变量、数量或时间的文案必须使用平台参数化、复数和区域格式能力，不得通过不利于翻译的字符串拼接生成完整句子。
- 日期、时间、数字和单位使用当前 App 语言格式。NAS 名称、用户名、文件名、聊天正文和服务器返回的用户数据保持原文。
- 增加语言时必须同步更新 `contracts/localization/supported-locales.json`、Apple、Android、Windows 资源、语言选择器、自动化测试、README 和平台矩阵。
- 提交前必须运行本地化资源完整性与硬编码扫描；新增缺失翻译、占位符不一致或用户界面硬编码时不得合并。
